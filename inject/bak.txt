/* 二级菜单（子目录）横向展开 */
#nav .menus_items .menus_item_child {
  display: none;              /* 默认隐藏 */
  position: absolute;
  top: 100%;
  left: 0;
  background: #fff;
  padding: 8px 12px;
  border-radius: 6px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.15);

  /* 横向排列 */
  display: flex;
  flex-direction: row !important;
  gap: 15px;
  white-space: nowrap;
}

/* 鼠标悬停时显示子菜单 */
#nav .menus_items > .menus_item:hover > .menus_item_child {
  display: flex !important;
}

#footer {
  background: transparent !important; /* 透明，跟随 body 背景 */
  box-shadow: none !important;        /* 去掉常规阴影 */
  border-top: none !important;        /* 去掉边框 */
}

/* 去掉页脚的伪元素阴影/遮罩 */
#footer::before,
#footer::after {
  display: none !important;
  background: none !important;
  box-shadow: none !important;
}